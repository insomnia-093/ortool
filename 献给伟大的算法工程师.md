## 献给伟大的算法架构师

### 1. 环境准备

```bash
# 1. 安装 Python 3.8+
python --version

# 2. 安装依赖库
pip install ortools pandas openpyxl

# 3. 数据格式 Excel
# 格式要求：包含【姓名、职位、科室、亚专科、上班次数（天数）】等列
```



### 2. 基础运行

```bash
# 自定义配置文件
python cs3.py --src-excel "D:\download\虚拟医生数据_300 人完整版.xlsx" --output-excel "排班结果.xlsx" --config "schedule_config.json"
```

### 3.我的需求

目前这是一个仅实现硬性约束的简单模型，我需要您不断优化算法，争取满足需求。如果能的话，我需要您通过我提供给您的表格伪造2到3个数据集（excel文件，直接ai生成就行）并在本地测试，有问题还望及时反馈。我会根据测试结果不断优化后端。

辛苦/(ㄒoㄒ)/~~